kind = "web"
previewPath = "/"
title = "Website Audit Report"
version = "1.0.0"
id = "artifacts/audit-report"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24587

[services.development]
run = "pnpm --filter @workspace/audit-report run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/audit-report", "run", "build" ]
publicDir = "artifacts/audit-report/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24587"
BASE_PATH = "/"
